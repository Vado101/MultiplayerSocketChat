plugins {
    id('common.java-conventions')
}

group = "${group}.client"

application {
    mainClass.set("${group}.Main")
    mainModule.set('socket.chat.client')
}

dependencies {
    implementation(project(':common'))
}